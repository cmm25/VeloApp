# ── Replit auto-attached files ───────────────────────────────────────────────
attached_assets/
.local/

# ── Node / TypeScript ────────────────────────────────────────────────────────
node_modules/
dist/
build/
*.tsbuildinfo
.npm

# ── Python ───────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
*.egg-info/
.eggs/
dist/
*.dist-info/

# ── Environment & secrets ────────────────────────────────────────────────────
.env
.env.local
.env.*.local
# keep .env.example files (they are templates, not secrets)
!.env.example

# ── Hardhat / Foundry ────────────────────────────────────────────────────────
Hardhat/artifacts/
Hardhat/cache/
Hardhat/typechain-types/
Hardhat/coverage/
Hardhat/coverage.json

# ── OS & editor ──────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
*.swo
.idea/
.vscode/
*.code-workspace

# ── Logs ────────────────────────────────────────────────────────────────────
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── Misc ─────────────────────────────────────────────────────────────────────
*.pdf
*.zip
*.tar.gz
tmp/
temp/

# ── Replit (environment config — kept local, not committed) ───────────────────
.replit
replit.nix

# ── Agent memory (local, not committed) ───────────────────────────────────────
.agents/

# ── Reference folders  ───────────────────
scripts/
/reference-files-eshaan/
.agents/
.docs/

# ── ML model weights & training artifacts (never commit binaries) ─────────────
*.pt
*.onnx
*.engine
*.tflite
runs/
lib/velo-engine/overlays/
lib/velo-engine/custom_models/*
!lib/velo-engine/custom_models/.gitkeep

# ── Local planning docs + verification scratch (kept local, not committed) ─────
.claude/
docs/VELO-INTEGRATION-PLAN.md
docs/VELO-ONCHAIN-HASH-MIGRATION.md
docs/HANDOFF_external_ai_models*.md
lib/velo-agents/verify_*.ts
