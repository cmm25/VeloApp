# Velo training — environment

# Roboflow private API key — app.roboflow.com → Settings → API
ROBOFLOW_API_KEY=

# Modal: `pip install modal && modal token new` handles auth (no key needed here).
# RunPod: just run train.py --local on the pod after syncing data/merged.
